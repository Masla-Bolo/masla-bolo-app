import 'package:flutter/material.dart';

class OfficialIssues extends StatelessWidget {
  const OfficialIssues({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("OFFICIAL ISSUES HERE")],
    );
  }
}
